# Local dev overrides. Database from docker-compose.yml.
spring:
  jpa:
    show-sql: true
  flyway:
    clean-disabled: false

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
